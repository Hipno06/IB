#include <iostream>
#include <cmath>

int Biseccion(numero1, numero2, valor_minimo, funcion) {
  double errores{0}, resultado{0};
  int m{0};
  std::cout << "| m | a_m | b_m | x_m+1 | f(x_m+1) | Errores |"
  do {
    errores=(numero1+numero2)/pow(2, m);
    std::
    m++;
  } while (errores < valor_minimo);



}
